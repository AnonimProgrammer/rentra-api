meta {
  name: Get Agency Memberships
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/v1/agencies/{{agencyId}}/memberships
  body: none
  auth: inherit
}

params:query {
  ~cursor: {{cursorId}}
  ~limit: {{limit}}
  ~role: FRONT_AGENT
  ~status: ACTIVE
}
