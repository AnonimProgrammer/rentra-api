meta {
  name: Confirm Reservation
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/v1/reservations/{{vehicleId}}/confirm
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "customerId": "{{customerId}}"
  }
}
